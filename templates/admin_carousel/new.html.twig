{% extends 'base-backend.html.twig' %}

{% block title %}Nouvelle image{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center">
        <h1 class="mt-3">Ajouter une nouvelle image</h1>
        <a href="{{ path('app_admin_carousel_index') }}" class="btn btn-dark btn-sm" title="Annuler">Annuler</a>
    </div>
    {{ include('admin_carousel/_form.html.twig') }}

{% endblock %}
